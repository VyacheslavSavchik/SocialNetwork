import {UsersType} from "../Redux/users-reducer";

// export const updateObjectInArray = (items: UsersType[], itemId: string, objPropName: string, newObjProps: {}) => {
//     return items.map(u => {
//         if (u.objPropName === itemId) {
//             return {...u, ...newObjProps}
//         }
//         return u
//     })
// }